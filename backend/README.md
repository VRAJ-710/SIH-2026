# /backend

This directory contains the FastAPI backend application.

## Role
- Serves unified query endpoints for instruments and grid variables.
- Implements a plugin registry for live or stub dataset ingestors.
- Interfaces with the database and reads standardized static file catalogs.
- Provides access to in-situ point data (Argo, Buoys, Gliders) and metadata.
